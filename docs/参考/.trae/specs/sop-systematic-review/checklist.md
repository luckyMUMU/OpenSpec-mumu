# SOP 体系系统性审查检查清单

## 一、内容重复检查

### 1.1 状态定义重复
- [ ] 状态字典中的所有状态定义是唯一真源（SSOT）
- [ ] 其他文档引用状态时使用"参见状态字典"格式
- [ ] 无文档在正文中完整重复定义状态

### 1.2 命令定义重复
- [ ] 命令字典中的所有命令定义是唯一真源（SSOT）
- [ ] 其他文档引用命令时使用"参见命令字典"格式
- [ ] 无文档在正文中完整重复定义命令

### 1.3 流程描述重复
- [ ] 工作流文档（03_workflow/）是流程描述的唯一真源
- [ ] AGENT_SOP.md中的流程描述为摘要+链接形式
- [ ] Skill矩阵中的流程描述为摘要形式

### 1.4 约束规则重复
- [ ] 约束矩阵（constraint_matrix.md）是约束规则的唯一真源
- [ ] 其他文档引用约束时使用"参见约束矩阵"格式
- [ ] 无文档在正文中完整重复定义约束规则

## 二、逻辑贯通性检查

### 2.1 状态转移一致性
- [ ] 所有SKILL.md中的停止点与状态字典定义一致
- [ ] 状态转移路径在所有文档中描述一致
- [ ] 门控状态转移规则完整定义

### 2.2 命令参数一致性
- [ ] 所有文档中使用的命令参数与命令字典定义一致
- [ ] 命令返回值类型定义一致
- [ ] 命令前置/后置状态定义一致

### 2.3 工作流连贯性
- [ ] 调用链中的所有Skill都存在于Skill矩阵
- [ ] 调用链顺序与Skill依赖关系一致
- [ ] 每个路径的入口和出口定义清晰

### 2.4 引用路径正确性
- [ ] 所有内部引用链接指向存在的文件
- [ ] 引用路径格式统一（基于SOP_ROOT的相对路径）
- [ ] 无断裂的引用链接

## 三、过时内容检查

### 3.1 废弃状态使用
- [ ] 无文档使用`[WAITING_FOR_TEST_REVIEW]`（已弃用）
- [ ] 无文档使用`[USER_DECISION_REQUIRED]`（已弃用）
- [ ] 所有状态使用当前标准名称

### 3.2 历史别名清理
- [ ] 无文档使用"设计参考"（应使用"设计依据"）
- [ ] 无文档使用"设计选择"（应使用"设计决策"）
- [ ] 无文档使用"Code Review"（应使用"代码审查"）

### 3.3 过时模板识别
- [ ] prd.md模板标记为不推荐使用
- [ ] 项目优先使用project_prd.md模板
- [ ] 过时模板有明确的替代说明

### 3.4 废弃命令检测
- [ ] 无文档使用已移除的命令
- [ ] 命令别名使用符合规范

## 四、版本一致性检查

### 4.1 核心文件版本
- [ ] AGENT_SOP.md版本号与CHANGELOG最新版本一致
- [ ] 01_concept_overview.md版本号一致
- [ ] 状态字典版本号一致
- [ ] 命令字典版本号一致

### 4.2 Skill合约版本
- [ ] 所有17个SKILL.md版本号一致
- [ ] Skill矩阵版本号与SKILL.md一致

### 4.3 依赖版本声明
- [ ] SKILL.md中的依赖版本声明正确
- [ ] 依赖版本满足当前版本要求

## 五、术语统一性检查

### 5.1 设计相关术语
- [ ] 统一使用"设计依据"而非"设计参考"
- [ ] 统一使用"设计决策"而非"设计选择"
- [ ] 统一使用"实现设计"而非"L3设计"

### 5.2 流程相关术语
- [ ] 统一使用"代码审查"而非"Code Review"
- [ ] 统一使用"验收审查"而非"交付审查"
- [ ] 统一使用"门控检查"而非"Gate Check"

### 5.3 状态相关术语
- [ ] 统一使用`[USER_DECISION]`而非`[USER_DECISION_REQUIRED]`
- [ ] 统一使用`[已完成]`而非`[COMPLETED]`
- [ ] 统一使用`[WAITING_FOR_ACCEPTANCE_REVIEW]`或带参数形式

## 六、文档结构检查

### 6.1 入口文档结构
- [ ] AGENT_SOP.md包含快速分诊表格
- [ ] 包含核心约束摘要
- [ ] 包含路径选择指引
- [ ] 包含导航表格

### 6.2 Skill合约结构
- [ ] 所有SKILL.md包含frontmatter（name, version, layer）
- [ ] 包含侧重点章节
- [ ] 包含触发条件
- [ ] 包含输入输出定义
- [ ] 包含停止点定义
- [ ] 包含约束章节

### 6.3 索引文档结构
- [ ] 05_constraints/index.md包含文档列表
- [ ] 04_reference/index.md包含模板和格式列表
- [ ] skills/index.md包含Skill链接列表

## 七、模拟编程场景验证

### 7.1 快速路径场景验证
- [ ] 从AGENT_SOP.md入口能快速找到快速路径指引
- [ ] sop-code-explorer → sop-code-implementation → sop-code-review调用链完整
- [ ] `[WAITING_FOR_CODE_REVIEW]` → `[DIFF_APPROVAL]`状态转移清晰
- [ ] code_audit_report.md模板存在且完整
- [ ] worker_execution_result.md模板存在且完整
- [ ] code_review.md模板存在且完整

### 7.2 深度路径场景验证
- [ ] 从入口能找到深度路径指引
- [ ] sop-requirement-analyst → sop-architecture-design → ...调用链完整
- [ ] 每个阶段的停止点定义清晰
- [ ] 门控检查流程可执行
- [ ] 文档模板引用链完整

### 7.3 多目录并行场景验证
- [ ] LIST_DESIGN_MD → SCHEDULE_DIRS → RUN_DIR_BATCH流程清晰
- [ ] 跨目录依赖处理流程可执行
- [ ] `[DIR_WAITING_DEP]`状态唤醒机制明确
- [ ] sop-progress-supervisor职责边界清晰

### 7.4 审查失败场景验证
- [ ] 三错即停机制可执行
- [ ] `[FUSION_TRIGGERED]`状态处理流程完整
- [ ] `[USER_DECISION]`选项明确
- [ ] 决策记录模板可用

### 7.5 门控失败场景验证
- [ ] `[GATE_FAILED]`状态处理流程完整
- [ ] 用户决策选项（修复后重试/回滚/终止）明确
- [ ] GATE_RETRY命令在命令字典中定义
- [ ] GATE_ROLLBACK命令在命令字典中定义

### 7.6 Spec模式交互场景验证
- [ ] 冲突检测触发条件清晰
- [ ] ADR确认更新机制可执行
- [ ] 决策分级记录规则明确

### 7.7 中断恢复场景验证
- [ ] 可恢复检查点列表完整
- [ ] continuation_request模板存在且可用
- [ ] 恢复所需最小输入明确

## 八、引用流畅性验证

### 8.1 入口到详情的引用流畅性
- [ ] 快速分诊表格中的链接能直达目标文档
- [ ] 目标文档包含返回入口的导航
- [ ] 层级导航（L1→L2→L3→L4）顺畅

### 8.2 跨文档引用流畅性
- [ ] 引用路径正确
- [ ] 被引用内容存在于目标位置
- [ ] 无循环引用

### 8.3 模板引用流畅性
- [ ] 模板路径正确
- [ ] 模板内容完整
- [ ] 模板包含必要的字段定义

## 九、审查报告确认

### 9.1 问题分类
- [ ] 高优先级问题已标记
- [ ] 中优先级问题已标记
- [ ] 低优先级问题已标记

### 9.2 修复建议
- [ ] 每个问题有明确的修复建议
- [ ] 修复建议可操作

### 9.3 统计信息
- [ ] 内容重复数量已统计
- [ ] 引用施数量已统计
- [ ] 过时内容数量已统计
- [ ] 版本不一致数量已统计

### 9.4 场景模拟结果
- [ ] 场景模拟验证结果已记录
- [ ] 发现的可用性问题已汇总
- [ ] 流程不顺畅的位置已标记
