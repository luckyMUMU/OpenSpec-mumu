## 范围澄清（按你的最新要求）
- SOP 面向 **AI 编码** 与交付可复核产物，不覆盖发布/运维/上线治理。
- 优化重点：对齐常见流程中的“可追溯性/依赖管理/变更决策记录”，并把它变成 SOP 的强约束。

---

## 对比：SOP vs 常见软件开发流程（只保留与 AI 编码相关的部分）

### 1) 常见流程的共同点（抽象层）
- 不同方法论（SDLC/敏捷/看板/TDD）在“编码之前”通常都强调：
  - 需求与目标来源可追溯
  - 设计输入有来源、输出能作为下一阶段依赖
  - 缺口需要显式决策（继续/补全/降级/搁置）

### 2) SOP 已覆盖得很强的部分
- 强 gate：`[WAITING_FOR_REQUIREMENTS]` / `[WAITING_FOR_ARCHITECTURE]` / `[WAITING_FOR_DESIGN]` / 代码审查 / 分层验收审查点（利于 AI 防漂移）。
- 强角色隔离：Tester/CodeReviewer/TestWorker 的权限边界（利于质量控制）。
- 命令式 DSL + SSOT（状态/命令字典）让流程可验证。

### 3) SOP 相对常见流程的关键缺口（与你的要求一致）
- **来源/依赖未被强制结构化**：
  - 虽然 SOP 隐含“后续依赖前置产出”，但缺少统一的“输入来源声明”和“依赖缺失时的中断策略”。
- **缺口决策缺少固定落盘模板**：
  - 当前有 `[USER_DECISION]`，但没有强制“决策必须落盘到哪、用什么格式、如何被后续引用”。

---

## 优化准则（新增为 SOP 约束）

### 准则 A：分析与设计必须声明来源与依赖
- Analyst：除“需求来源主要来自用户”外，任何补充（历史文档/代码现状/约束/行业规范）都必须在产物中列为来源。
- Prometheus/Oracle/Tester/Worker/TestWorker/CodeReviewer：其输入必须来自**前置阶段产出**（或被显式批准的外部来源），并在产物中声明依赖清单。

### 准则 B：找不到来源或依赖时必须中断询问用户，并记录决策
- 中断方式：进入 `[USER_DECISION]`（或新增更语义化的停止点）并给出选项。
- 记录方式：必须落盘“来源/依赖缺口与用户决策”文档，并在后续产物引用该决策记录。

---

## 落盘交付物设计（新增模板/SSOT）

### 1) 新增交互格式：来源与依赖声明（可复用模板）
- 新文件：`04_reference/interaction_formats/source_dependency.md`
- 内容包含：
  - Inputs（列出前置产物路径/版本/摘要）
  - External Sources（可选，必须可复核）
  - Dependencies（显式依赖项）
  - Gaps（缺口）
  - Decision Record（若进入用户决策）

### 2) 新增审查标准：来源/依赖合规性
- 新文件：`04_reference/review_standards/source_dependency.standard.md`
- 强制点：任何 L2/L3/测试设计/审查报告，都必须包含来源/依赖声明；缺失视为 🔴。

### 3) 决策落盘位置（持久化）
- 推荐：写入 `docs/04_context_reference/`（ADR/decision record）或写入 `sop/reviews/YYYY-MM-DD/` 的决策记录文件。
- 规则：后续产物必须引用该决策文件路径。

---

## SOP 文件需要更新的点（你确认后我会执行）

### 1) SSOT 级别更新
- `05_constraints/command_dictionary.md`
  - 增加命令：`TRACE_SOURCES(inputs) -> source_dependency`、`RECORD_DECISION(topic, decision) -> decision_record`
- `05_constraints/state_dictionary.md`
  - 方案 1（推荐）：复用 `[USER_DECISION]`，并规定当 topic=SOURCE/DEP_MISSING 时必须落盘 decision_record
  - 方案 2：新增停止点（例如 `[WAITING_FOR_SOURCES]`），语义更清晰

### 2) 核心流程文件更新
- `03_workflow/index.md` / `03_workflow/deep_path.md`
  - 在 Analyst/Prometheus/Oracle/Tester/Worker/TestWorker/CodeReviewer 阶段补充“来源/依赖声明”作为必做步骤

### 3) 角色 Prompt/Skill 对齐
- Prompts：`analyst_prompt.md`、`prometheus_prompt.md`、`oracle_prompt.md`、`tester_prompt.md`、`worker_prompt.md`、`test_worker_prompt.md`、`code_reviewer_prompt.md`
  - 增加统一段落：
    - “Sources & Dependencies（必填）”
    - “Missing source/dep → 必须进入用户决策并落盘”
- Skills：对涉及审查/设计产出的 Skill 增加对 `source_dependency.standard.md` 的引用（与现有 review_standards 体系一致）。

---

## 输出：优化报告（比较 + 变更清单）
- 新增一份优化报告落盘：`docs/参考/sop/reviews/2026-02-11/10_sop_optimization_report_sources_dependencies.md`
  - 内容包括：对比摘要、缺口、准则、SSOT/文档变更清单、回归检查项。

---

## 回归与验证
- 扫描 SOP 全库：确保所有“设计/审查/测试设计”类文档与 Prompt/Skill 均能引用到新模板/新标准。
- 给出回归结果：仍缺失来源/依赖声明的位置清单（若有）。