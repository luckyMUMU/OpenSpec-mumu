按照您的指示，我将执行以下操作以确保文档结构描述与实际文件系统一致，并增强规约的充分性：

### 1. 修正 `sop_GUIDE.md` 中的文件结构描述

当前 `sop_GUIDE.md` (Section 2.1) 将自己描述为 `sop/` 目录下的文件，这与实际情况（位于 `docs/参考/`）不符。我将调整该树状图，使其反映真实结构：

```text
docs/参考/
├── sop_GUIDE.md              # 本文件：SOP 编写指南
├── sop_for_human.md          # 人类阅读版规约
└── sop/                      # AGENT SOP 核心目录
    ├── AGENT_SOP.md          # 入口文档
    ├── ...
```

### 2. 增强 `sop_GUIDE.md` 的 AGENT 交互规约

为了确保规约“充分且适于 AGENT”，我将在 `sop_GUIDE.md` 中新增 **"3.4 AGENT Prompt 编写标准"**。这部分内容将**仅包含格式约束**（不包含 SOP 业务内容），例如：
- **变量语法**：统一使用 `[PLACEHOLDER]` 格式。
- **输出锚点**：强制要求定义清晰的 Markdown 标题锚点（如 `## Conclusion`），便于自动化解析。
- **思维链规范**：要求复杂任务必须包含思考过程步骤。

### 3. 验证 `sop_for_human.md` 一致性

我已对比 `sop_for_human.md` 与 `docs/参考/sop/` 的内容：
- **角色一致性**：双方均包含 9 个核心角色。
- **流程一致性**：快速/深度路径定义一致。
- **三错即停**：规则定义一致。

目前 `sop_for_human.md` 与 AGENT SOP 保持高度一致，**无需修改**。

### 总结执行计划

1.  **编辑** `d:\Code\AI\OpenSpec-mumu\docs\参考\sop_GUIDE.md`：
    - 更新 `2.1 文件组织` 章节的目录树。
    - 新增 `3.4 AGENT Prompt 编写标准` 章节。

是否确认执行？