我将按照您的要求，在 `d:\Code\AI\OpenSpec-mumu\docs\参考\sop_GUIDE.md` 中新增“少即是多”约束，并全面贯彻“使用模板而非示例”的原则。

### 执行计划

1.  **新增核心原则 (1.4 少即是多)**
    - 在 `1. 核心原则` 章节末尾添加 `1.4 少即是多 (Less is More)`。
    - 明确规定：减少不必要的描述，**禁止使用具体示例 (Example)**，**强制使用模板 (Template)**。
    - 添加对比表格，直观展示“模板”与“示例”的区别。

2.  **更新 L4 参考层定义**
    - 将 `L4 参考层` 内容描述中的 `示例代码` 修改为 `代码模板`。

3.  **更新审核清单 (4.2 审核清单)**
    - 在 `内容审核` 部分增加检查项：`[ ] 是否使用模板代替了具体示例`。

### 修改预览

**新增的 1.4 章节：**
```markdown
### 1.4 少即是多 (Less is More)

SOP 应聚焦于结构和规则，而非具体内容的演绎：

- **拒绝冗余**：删除所有不影响执行的修饰性文字、背景介绍或“废话”。
- **模板 > 示例**：
  - **禁止**：编写具体的业务示例（Example），避免 Agent 过拟合或模仿具体的业务逻辑。
  - **必须**：提供高度抽象的模板（Template），使用占位符明确数据结构。

| 维度 | 推荐 (Template) | 不推荐 (Example) |
|------|----------------|------------------|
| **代码** | 包含 `[PLACEHOLDER]` 的框架 | 具体的 `Hello World` 实现 |
| **指令** | `生成 [Language] 接口` | `生成 Python 接口` |
| **格式** | `key: [value]` | `name: John` |
```

是否确认执行上述修改？