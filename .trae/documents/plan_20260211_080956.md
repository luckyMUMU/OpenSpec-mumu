基于审查报告 (Review Report)，制定以下修复计划以解决 **文档缺失** 和 **合规性缺失** 问题。

### 修复目标
1.  恢复 SOP 的完整性 (重建 `sop_for_human.md`)。
2.  确保所有核心产物模板符合 "来源与依赖" 强制合规要求。
3.  修复文档间的断链问题。

### 执行步骤

#### 1. 重建核心文档 (High Priority)
-   **任务**: 重建 `docs/参考/sop/sop_for_human.md`
-   **依据**: 基于 `AGENT_SOP.md` 的逻辑架构，参照 `sop_GUIDE.md` 的章节要求。
-   **内容**:
    -   包含完整的角色、工作流、停止点定义。
    -   **新增**: 第16章 "来源与依赖合规"，明确声明所有产物必须追溯来源。

#### 2. 模板合规性升级 (High Priority)
-   **任务**: 批量更新 `docs/参考/sop/04_reference/document_templates/` 下的核心模板。
-   **操作**: 在以下模板的头部或尾部插入 `Source and Dependency` 标准区块 (引用 `source_dependency.md`)：
    1.  `project_prd.md` (L1)
    2.  `module_mrd.md` (L2)
    3.  `feature_frd.md` (L3)
    4.  `architecture_design.md` (L2 Design)
    5.  `implementation_design.md` (L3 Design)
    6.  `adr.md` (决策记录)

#### 3. 链接与路径修复 (Medium Priority)
-   **任务**: 修正 `AGENT_SOP.md` 和 `deep_path.md` 中的路径错误。
-   **操作**:
    -   `AGENT_SOP.md`: 将绝对路径 `file:///d:/Code...` 替换为相对路径。
    -   `deep_path.md`: 修正 `design_directory_strategy.md` 的相对引用层级。

#### 4. 验证
-   **操作**: 再次运行链接检查，确认所有修复后的文档链接有效。

### 预期产出
修复完成后，SOP 文档库将重新符合 `sop_GUIDE.md` v1.5.0 标准，且所有新建的文档都将强制包含来源追踪信息。