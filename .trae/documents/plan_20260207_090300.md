我对 `d:\Code\AI\OpenSpec-mumu\docs\参考\sop` 目录进行了系统性审查，对照新版 `sop_GUIDE.md`（特别是“少即是多”和“Agent 交互规范”），发现以下关键问题：

### 审查报告

#### 🔴 关键违规 (Critical Violations)
1.  **违反“模板 > 示例”原则**：
    - 多个 Skill 和 Role 文件包含具体的业务示例（如 `analyst.md` 和 `SKILL.md` 中的“用户认证功能”示例）。
    - **风险**：Agent 容易过拟合这些示例，导致在处理非相关业务时产生幻觉或生搬硬套。
2.  **Agent Prompt 规范缺失**：
    - `prompts/analyst_prompt.md` 等 Prompt 文件未包含 `## Thinking Process`（思维链）要求。
    - 变量占位符格式不统一（混用 `[feature]` 和自然语言描述），未严格遵循 `[PLACEHOLDER]` 标准。
3.  **信息冗余**：
    - Role 文档（如 `analyst.md`）大量重复 Workflow 文档（`deep_path.md`）中的流程描述，违反“单一来源”原则。

#### ✅ 符合项 (Compliance)
1.  **文档模板**：`04_reference/document_templates/prd.md` 质量很高，已经完全使用了 `[PLACEHOLDER]` 格式，是符合新规约的典范。
2.  **目录结构**：完全符合 L1-L4 分层结构。

---

### 修复与优化计划

建议执行以下操作以全面对齐新规约：

#### 1. 清洗 Role 和 Skill 文档 (Less is More)
- **动作**：删除 `02_role_matrix/*.md` 和 `skills/**/*.md` 中的所有具体业务示例（Example）。
- **替代**：替换为指向 `04_reference` 中对应模板的引用，或使用抽象的逻辑模板。
- **去重**：简化 Role 文档中的“工作流”部分，改为引用 `03_workflow` 中的权威定义。

#### 2. 升级 Agent Prompts (Agent Specs)
- **动作**：重写 `prompts/*.md`。
- **新增**：添加 `## Thinking Process` 章节，强制 Agent 在输出前进行思维链推导。
- **标准化**：将所有变量统一为 `[PLACEHOLDER]` 格式。
- **结构化输出**：明确定义输出的 Markdown 锚点（如 `## Output`）。

#### 3. 统一 Skill 定义
- **动作**：更新 `skills/**/*.md`。
- **移除**：删除冗余的“角色关系图”（已在 L2/L3 定义）。
- **增强**：确保 Input/Output 格式使用严格的 Template 语法。

是否确认执行此修复计划？